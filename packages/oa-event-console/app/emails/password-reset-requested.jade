include _template

block content
  p A password reset was requested for your email address.
  p Follow this link to set your new password:
  a(href="#{url}/password/reset/#{token}") #{url}/password/reset/#{token}