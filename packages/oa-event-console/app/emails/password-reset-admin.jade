include _template

block content
  p Your console administrator has reset your password
  p Follow this link to set your new password: 
  p #[a(href="#{url}/password/reset/#{token}") #{url}/password/reset/#{token}]
  p If you have any issues with the link you can #[a(href="#{url}/password/request") request another reset]