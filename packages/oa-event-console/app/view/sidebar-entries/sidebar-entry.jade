.sidebarEntry( 
  data-time="{{ time }}"
  data-event="{{ event }}"
  data-ids="{{ ids_str }}"
  data-identifiers = "{{ identifiers }}")

  .content
    //.avatar
    .message {{& message }}
    .time.pull-right(title="{{ time }}") just now
